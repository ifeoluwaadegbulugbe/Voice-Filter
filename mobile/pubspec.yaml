name: voice_filter_ai
description: AI Voice Filter — Flutter mobile app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http:                 ^1.2.0
  record:               5.2.1
  audioplayers:         ^6.0.0
  file_picker:          ^8.0.0
  path_provider:        ^2.1.2
  shared_preferences:   ^2.2.2
  permission_handler:   ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:        ^3.0.0

dependency_overrides:
  record_linux:         1.3.0

flutter:
  uses-material-design: true
